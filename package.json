{
  "name": "praeforma",
  "version": "0.1.0",
  "private": true,
  "description": "LUDIARS Praeforma — spec ↔ implementation linkage tool (placeholder layout + structured spec + asset swap)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "build:web": "npm run build -w web",
    "local": "npm run build:web && npm run local -w server",
    "env:setup": "npm run env:setup -w server",
    "env:test": "npm run env:test -w server",
    "env:gen": "npm run env:gen -w server",
    "env:list": "npm run env:list -w server",
    "env:get": "npm run env:get -w server",
    "env:set": "npm run env:set -w server"
  }
}
